programa

  declare int a, b , i.
  declare texto  t1.

  leia (a).
  leia (b).
  
  a := 1 + 2 * 3 / b.    
  
  se ( a > b ) entao {
     escreva (a).
     escreva (b).
     escreva (a).
  }
fimprog.